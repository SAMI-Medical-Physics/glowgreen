require 
Python 3.9+ for return type hint tuple[]
sphinx (pip install sphinx)

sphinx-quickstart and follow prompts

copy overview.rst into docs/source

run glowgreen_sample.py in src and save figs in docs/source with filenames docs_cpat.png and docs_cpat_dose.png

in source/conf.py
import os
import sys
sys.path.insert(0, os.path.abspath('../..'))
sys.path.insert(0, os.path.abspath('../../src/'))
sys.path.insert(0, os.path.abspath('../../src/glowgreen/'))

extensions = ['sphinx.ext.autodoc', 'sphinx.ext.napoleon']

# to document __init__
autoclass_content = 'both'

html_theme = 'classic'

in docs cmd:
sphinx-apidoc -o ./source ../src/glowgreen/ -e -M

in index.rst:
write 'overview', 'modules' under toctree

Change heading in modules.rst to 'API reference'
and add '.. _API reference:' before the heading

in glowgreen.close_contact.rst:
write ':private-members: _ContactPattern' under automodule

in docs cmd: make html